# Test Fixtures

This directory is reserved for compact SUMO-like XML/JSON fixtures used by fast unit and integration tests.

Keep fixtures small and synthetic unless a test explicitly documents why a real artifact sample is needed. Full simulation outputs and playback JSON files should not be copied here.
